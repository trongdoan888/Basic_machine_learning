document.addEventListener( 'DOMContentLoaded', () => {
	( () => {
		const e = document.querySelector( '.sort-by-wrapper' );
		if ( ! e ) return;
		const t = e.querySelector( '.toggle' ),
			r = e.querySelector( 'ul' ).querySelectorAll( 'li' );
		[ ...r ].map( ( e ) => {
			e.addEventListener( 'click', function ( o ) {
				[ ...r ].map( ( e ) => {
					e.classList.remove( 'active' );
				} ),
					e.classList.add( 'active' ),
					( t.innerHTML = e.innerHTML );
			} );
		} );
	} )(),
		( () => {
			const e = document.querySelector( '.sort-by-wrapper' );
			e &&
				( document.querySelector( '#hb-form-search-page' ) ||
					[ ...e.querySelector( 'ul' ).querySelectorAll( 'li' ) ].map(
						( e ) => {
							e.addEventListener( 'click', function ( t ) {
								const r = new URL( window.location.href );
								r.searchParams.set(
									'sort_by',
									e.getAttribute( 'data-value' )
								),
									( window.location.href = r );
							} );
						}
					) );
		} )();
} );
